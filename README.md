# buy-or-not (살까 말까)

무신사 링크를 붙여넣어 가진 옷을 옷장으로 등록하고, 새로 살까 고민 중인 옷의 링크를 넣으면 옷장의 실측·선호도와 비교해 살만한지 판단해주는 개인 프로젝트. 취업 준비용 사이드 프로젝트로 진행 중이며, 겪은 문제와 해결 과정을 Phase 단위로 아래에 기록한다.

## 진행 기록

### Phase 0 — 무신사 파싱 타당성 조사 (2026-08-15)

**문제**: 무신사는 공식 API가 없다. 상품 페이지를 스크래핑해야 하는데, 특히 실측치(총장·가슴단면 등)를 어디서 얻을 수 있는지가 파서·저장 구조·등록 화면 UX를 전부 좌우해서, 코드를 짜기 전에 먼저 확인이 필요했다.

**조사 방법**: `curl`로 실제 상품 페이지(상의·하의·아우터 3개 카테고리)의 HTML을 받아 분석하고 `robots.txt`를 확인했다. 실측 데이터가 정적 HTML에 없어서, 브라우저에서 직접 페이지를 열어 개발자도구 Network 탭·Resource Timing API·`fetch`/`XHR` 몽키패치까지 동원해 그 값이 어떤 경로로 들어오는지 추적했다.

**발견**:
1. **robots.txt**: 이름이 지정된 봇(주요 검색엔진, AI 에이전트)만 `Allow`하고 나머지 User-Agent는 전부 `Disallow: /`. 이 프로젝트의 서버 fetcher는 이 허용 목록에 없다.
2. **상품 기본 정보**(이름·브랜드·가격·이미지·카테고리)는 서버가 내려주는 정적 HTML 안 `__NEXT_DATA__` 스크립트에 JSON으로 이미 들어있어서, 헤드리스 브라우저 없이 `fetch` 한 번으로 가져올 수 있었다.
3. **실측치 테이블**은 정적 HTML에도, 위 JSON에도, 관찰 가능한 어떤 네트워크 요청에도 나타나지 않았다 — 브라우저가 렌더링한 뒤에야 DOM에 나타나는데, 그 값이 어디서 오는지 끝내 특정하지 못했다.

**해결**: 실측치는 "무신사 페이지의 사이즈 표를 사용자가 통째로 복사해서 붙여넣으면, 클립보드 HTML 구조를 그대로 파싱해 자동으로 채워주는" 방식으로 설계를 바꿨다. 단순 수동 타이핑보다 마찰이 적고, 한 사용자가 붙여넣은 표는 캐시에 반영되어 다음 사용자는 이 단계 자체를 건너뛴다. Selenium 같은 헤드리스 브라우저 크롤러는 검토했지만 기각했다 — robots.txt 문제를 그대로 안고 가는 데다, 서버에 무거운 브라우저 인프라를 얹는 비용이 이 방식으로 얻는 이득보다 컸다.

**결과**: 설계 스펙과 구현 계획을 실제로 확인된 구조에 맞게 다시 썼다. 상품 기본 정보는 자동 파싱, 실측치는 사용자 붙여넣기 기반 자동 파싱으로 역할을 나누는 구조로 Phase 1(기반) 착수 전에 정리를 마쳤다.

### Phase 1 — 기반 (2026-08-15)

Next.js 프로젝트 셋업부터 Supabase 스키마·RLS·구글 로그인까지, 로그인해서 빈 옷장(아직 없는 페이지라 404)을 보는 것까지가 목표였다.

**겪은 문제 1 — 기존 파일과 `create-next-app`의 충돌**

**문제**: 리포에 이미 `CLAUDE.md`, `docs/`, `tests/` 같은 파일이 있는 상태에서 `create-next-app`을 돌리면, 도구가 "충돌 가능성이 있다"며 아예 실행을 거부한다.

**해결**: 임시 폴더에 스캐폴드를 생성한 뒤 `app/`, `package.json`, 설정 파일들만 리포 루트로 옮기고, 기존 문서·테스트 파일은 그대로 뒀다. `.gitignore`도 Next.js 기본 템플릿과 병합하면서 `.env*` 같은 넓은 패턴이 `.env.local.example`(커밋해야 하는 템플릿 파일)까지 가려버리지 않게 `.env`/`.env.local`/`.env*.local`로 좁혀서 썼다.

**겪은 문제 2 — Supabase 마이그레이션이 `gen_random_bytes`에서 실패**

**문제**: 가입 트리거 마이그레이션(`0003_profile_trigger.sql`)을 원격 Supabase 프로젝트에 적용하는데 `function gen_random_bytes(integer) does not exist` 오류가 났다.

**원인**: Supabase 호스팅 프로젝트는 `pgcrypto` 같은 공용 확장을 `public` 스키마가 아니라 `extensions` 스키마에 설치해둔다. 함수 본문에서 스키마를 명시하지 않고 `gen_random_bytes(9)`를 부르면, 그 시점의 `search_path`에 `extensions`가 없어서 함수를 찾지 못한다. (반면 `gen_random_uuid()`는 PostgreSQL 13+의 코어 내장 함수라 이 문제가 없었다 — 같은 마이그레이션 파일 안에서 하나는 되고 하나는 안 되는 이유였다.)

**해결**: `extensions.gen_random_bytes(9)`로 스키마를 명시적으로 붙였다. 로컬 Docker 없이 원격 프로젝트에 직접 `supabase db push`를 반복하며 잡은 문제라, 실제 호스팅 환경의 스키마 배치를 직접 겪어보지 않았다면 놓치기 쉬웠다.

**검증**: RLS 정책은 실제 원격 DB에 두 명의 테스트 유저(소유자/제3자)를 만들어 "남의 비공개 옷장 조회 차단", "공개 옷장 조회 허용", "추천만 가능하고 소유자 사칭은 불가", "남의 옷 수정·삭제 불가", "일반 사용자는 `musinsa_cache` 접근 불가" 등 9개 시나리오를 실제로 통과시켜 검증했다(정책 문구만 보고 맞다고 가정하지 않았다). 구글 로그인도 실제 OAuth 플로우를 끝까지 돌려 `/wardrobe`로 리디렉트되고 `profiles` 행이 트리거로 자동 생성되는 것까지 확인했다.

**결과**: Phase 1 완료 기준(로그인 → 빈 옷장 경로) 충족. Docker 없이 원격 Supabase 프로젝트만으로 개발 사이클(마이그레이션 작성 → `db push` → 테스트/수동 검증)을 완성했다.

### Phase 2 — 옷장 등록 (2026-08-15)

무신사 링크를 붙여넣어 색상·사이즈를 고르면 실측과 함께 옷장에 저장되고 그리드로 조회되는 것까지가 목표였다. 파싱 API·캐시(Task 9), 링크 입력·옵션 선택 화면(Task 10), 등록 API(Task 11), 옷장 그리드(Task 12) 순서로 진행했다.

**겪은 문제 — Supabase Storage가 한글 오브젝트 키를 거부**

**문제**: 실제 무신사 링크(`/products/6593921`)로 등록까지 전체 플로우를 브라우저에서 검증하는 중, `garments` 테이블에는 행이 정상적으로 쌓였지만 `image_url`이 Storage 사본 URL이 아니라 무신사 CDN 원본 URL 그대로 저장돼 있었다. `storage.objects`를 조회해보니 아예 파일이 하나도 없었다 — 이미지 복사(`copyImageToStorage`)가 매번 조용히 실패하고 있었다.

**원인**: 이 함수는 예외 대신 실패 시 저장을 막지 않으려고 실패를 전부 삼켜서(`try { … } catch { return null }`) 실패 자체는 등록을 방해하지 않지만, 원인도 함께 감춰버렸다. 별도 스크립트로 `copyImageToStorage`와 동일한 fetch→업로드 과정을 그대로 재현해 에러를 직접 찍어보니 `StorageApiError: Invalid key`였다. 오브젝트 경로를 만들 때 색상명(`색상 문자열 → 정규식으로 안전화`)에 쓴 정규식이 `[^\p{L}\p{N}]+`(유니코드 "문자·숫자"는 전부 허용)였는데, 한글도 유니코드 Letter 카테고리라 그대로 통과됐다. 문제는 Supabase Storage 쪽 — 오브젝트 키에 비-ASCII 문자(한글 포함)를 아예 허용하지 않는다. 무신사 색상 옵션은 "블랙"·"화이트"처럼 거의 항상 한국어라 이 경로는 사실상 항상 실패하는 구조였다.

**해결**: 색상명을 경로에 그대로 쓰는 대신 `crypto.createHash('sha1')`로 짧은 해시를 만들어 오브젝트 키(`{goodsNo}/{해시}.{확장자}`)를 구성하도록 `lib/storage.ts`를 고쳤다. 이 경로는 사용자에게 노출되지 않고(공개 URL만 노출된다) 같은 색상이면 항상 같은 해시가 나와 재등록 시 `upsert`로 재사용된다.

**검증**: 유닛 테스트·`npm run build`만으로는 이 버그가 드러나지 않았다(둘 다 네트워크·실제 Storage 호출이 없는 경로라서). 로그인된 브라우저 세션으로 실제 무신사 링크를 붙여넣어 끝까지 등록해보고, Supabase에 직접 SQL로 `garments`/`garment_measurements`/`storage.objects`를 조회해서야 잡아냈다 — 수정 후 같은 플로우를 다시 돌려 `image_url`이 Storage 공개 URL로 바뀌고 `storage.objects`에 파일이 생기는 것까지 확인했다. 테스트로 등록했던 옷 2벌과 이미지 파일은 검증 후 정리했다.

**겪은 문제 — 사이즈표 붙여넣기가 실제 무신사 페이지에서는 값이 한 칸씩 밀려서 들어감**

**문제**: 위 버그를 고친 뒤 실제 사용해보니, 사용자가 무신사 "사이즈" 탭 표를 직접 드래그해서 복사·붙여넣었는데도 사이즈별 실측값이 자동으로 채워지지 않았다. 붙여넣은 원문을 확인해보니 `cm / 내 사이즈 / M / L / XL`(라벨)이 먼저 나오고, 그 다음에 `총장 어깨너비 가슴단면 소매길이`(헤더)와 숫자 행들이 따로 이어지는, Task 5-1에서 가정했던 "한 표에 라벨 칸 포함" 구조와 다른 순서였다.

**원인**: 실제 상품 페이지를 열어 DOM을 직접 조사해보니(`document.querySelectorAll('table')`, `<li>` 탐색), 사이즈 라벨(cm/내 사이즈/M/L/XL)은 `<table>`이 아니라 `<ul><li>`로 만든 별도의 고정(sticky) 열이고, 실측값은 라벨 칸이 아예 없는 별도의 `<table>`이었다 — 표 폭이 넓어질 수 있는 하의 등에서 첫 열을 고정하기 위한 흔한 UI 패턴으로 보인다. Phase 0 조사 때는 "실측표는 정적 HTML에 없다"까지만 확인했을 뿐, 사용자가 실제로 드래그해서 복사했을 때 이 두 DOM이 어떤 순서로 클립보드에 담기는지는 확인하지 못한 상태였다. 기존 파서(`rowsFromHtml`)는 `<tr>`만 찾아서 `<li>` 라벨 자체를 통째로 무시했고, 표 조립 로직(`buildTable`)은 "헤더 첫 칸=라벨, 데이터 행 첫 칸=사이즈명"이라고 잘못 가정해서 총장 값이 통째로 날아가고 숫자(61 등)가 사이즈명으로 오인되는 결과를 냈다.

**해결**: `lib/musinsa/pasteSizeTable.ts`를 실제 구조에 맞춰 다시 짰다. HTML 파싱 시 `<tr>`뿐 아니라 `<li>`도 문서 순서 그대로 수집하고, "칸이 1개뿐인 행 = 라벨 후보", "헤더와 칸 수가 같고 숫자가 있는 행 = 측정 데이터"로 나눠 각각 모은 뒤, 라벨 목록을 뒤에서부터(= `cm`·`내 사이즈` 같은 실제 사이즈가 아닌 항목이 자연히 걸러지도록) 데이터 행 개수만큼 잘라 순서대로 짝지었다.

**검증**: 실제 상품 페이지의 사이즈 탭 DOM을 브라우저 자바스크립트로 직접 읽어 라벨(`<li>`)·측정값(`<table>`) 구조를 확인한 뒤, 그 구조를 그대로 재현한 테스트 픽스처로 유닛 테스트를 다시 작성해 9개 모두 통과시켰다. 브라우저 자동화로 실제 Ctrl+C/Ctrl+V 클립보드 이벤트까지 재현하려 했으나 페이지가 자동화 드래그 중 응답 없음 상태가 되어 끝까지 재현하지 못했다 — 이 부분은 사용자가 직접 다시 붙여넣어 확인하기로 했다.

**결과**: Phase 2 완료 기준(무신사 링크 → 옵션 선택 → 실측 저장 → Storage 이미지 복사 → 옷장 그리드 표시) 충족. `npm test`(53개) 전부 통과, `npm run build` 타입 오류 없이 성공. Phase 0에서 "확인 안 됨"으로 남겨뒀던 부분(실측표를 실제로 복사했을 때의 클립보드 구조)이 사용자 실사용 중에 드러난 사례 — 사전 조사만으로는 잡을 수 없고 실제 사용 피드백이 필요한 종류의 문제였다.

### Phase 3 — 옷장 상세와 선호도 (2026-08-15)

옷 상세 화면에서 실측을 보고, 별점·핏 태그·착용빈도를 남기고, 삭제할 수 있는 것까지가 목표였다. `PATCH`/`DELETE /api/garments/:id`와 `/wardrobe/[id]` 페이지를 만들었다.

**겪은 문제 — Supabase JS의 `update()`/`delete()`에 `count` 옵션을 잘못된 위치에 넘김**

**문제**: "몇 행이 갱신·삭제됐는지"로 남의 옷(RLS가 막아 0행 처리됨)과 존재하지 않는 옷(404)을 구분하려고 `.update(values).eq('id', id).select('id', { count: 'exact', head: true })`처럼 `select()`에 `count` 옵션을 얹었더니 `npm run build`에서 "Expected 0-1 arguments, but got 2" 타입 에러가 났다.

**원인**: `@supabase/postgrest-js`의 소스(`node_modules/@supabase/postgrest-js/src/PostgrestQueryBuilder.ts`)를 직접 열어보니, `count`는 `update()`/`delete()`/`insert()`/`upsert()` 자신이 두 번째 인자로 받는 옵션이었다 — 뒤에 체이닝하는 `select()`는 그 옵션을 받지 않는다. `select()`에 얹는 건 최초 조회(SELECT) 쿼리에서 count를 요청할 때 쓰는 패턴이라, mutation 뒤에 붙이면 형태가 다르다.

**해결**: `count`를 `update(updates, { count: 'exact' })`, `delete({ count: 'exact' })`처럼 mutation 호출 자체에 옮겼다. `select()` 체이닝 자체가 필요 없어져 코드도 한 줄 줄었다.

**검증**: 브라우저 콘솔에서 실제 로그인 세션으로 PATCH(선호도 수정 성공/404/이미 owned인 옷 재승격 차단)와 DELETE(성공/404)를 각각 호출해 응답 코드를 확인했다. 상세 화면에서는 별점·핏·착용빈도 클릭 → 즉시 저장 → 새로고침 유지, 삭제 확인 → `/wardrobe`로 리다이렉트 → 그리드에서 사라짐까지 실제로 눈으로 확인했다.

**결과**: Phase 3 완료. `npm test`(84개) 전부 통과.

### Phase 4 — 핏 판단 엔진 (2026-08-15)

`lib/fit/rules.ts`(허용편차·심각도·가중치 상수), `lib/fit/profile.ts`(선호 실측 범위 클러스터링), `lib/fit/engine.ts`(편차 채점), `lib/verdict.ts`(최종 판정)를 AI 없이 순수 함수로 만들고, `/api/analyze`·`/analyze`·`/cart` 화면까지 붙여 "AI 없이 이미 쓸 만한 제품"(스펙 §14)을 완성하는 게 목표였다.

**겪은 문제 — 직접 만든 테스트의 기대값이 스펙 공식을 잘못 계산함**

**문제**: `scoreDeviation`(편차 채점) 테스트 두 개가 실패했다. "범위 밖으로 2cm 초과했으니 경고 점수" 같은 케이스에서 기대한 점수가 실제 결과와 달랐다.

**원인**: 스펙의 편차 공식은 `[lo, hi]`(선호 범위) 자체가 아니라 `[lo - t, hi + t]`(허용 편차만큼 넓힌 구간) 밖으로 벗어난 만큼을 `excess`로 잡는다. 테스트를 짤 때 이 확장 구간을 빼먹고 원래 범위 `[lo, hi]` 기준으로 "2cm 초과"를 계산해 값을 골랐다 — 실제로는 허용 편차(예: 총장 t=3.0)만큼 이미 여유가 있어서, 그 값은 여전히 허용구간 안이라 0점이 나오는 게 정답이었다. 코드(`lib/fit/engine.ts`)가 틀린 게 아니라 테스트 예시 수치가 스펙 공식을 잘못 대입한 경우였다.

**해결**: 허용구간 `[lo-t, hi+t]`을 먼저 계산한 뒤 그 밖으로 벗어나는 값을 역산해서 테스트 수치를 다시 골랐다(경고: 허용구간 밖 2cm, 심각: 허용구간 밖 5cm). 계획 문서의 테스트 예시 코드도 같은 실수가 있어 함께 고쳤다.

**겪은 문제 — Supabase JS의 `.returns<T>()`가 deprecated**

**문제**: `lib/fit/profile.ts`의 DB 집계 쿼리에 `.returns<GarmentRow[]>()`를 썼더니 에디터에 deprecated 경고가 떴다(빌드는 통과하지만).

**해결**: 같은 라이브러리 소스에서 `@deprecated Use overrideTypes<yourType, { merge: false }>() method` 안내를 확인하고 `.overrideTypes<GarmentRow[], { merge: false }>()`로 교체했다. `/cart` 화면의 비슷한 캐스팅도 처음부터 이 방식으로 맞춰 작성했다.

**검증**: `lib/fit/rules.ts`·`profile.ts`(클러스터링+선호 프로필)·`engine.ts`(채점)·`verdict.ts`를 단위 테스트로 촘촘히 검증(스펙 §9의 크롭/오버핏 다중 범위 예시, 회피 신호, 치명 위반, 데이터 부족 분기 포함)한 뒤, `/api/garments`가 옷장/장바구니 등록 로직을 공유하도록 리팩터(`lib/garments/register.ts`)했다. 실제 무신사 링크로 `/analyze`에서 판단 → 배지·"데이터 부족" 안내 확인 → `/cart`에서 방금 분석한 옷 확인 → "샀어요" → `/wardrobe`로 이동까지 브라우저에서 전체 플로우를 검증했다.

**결과**: Phase 4 완료 기준(AI 없이 결정론적 리포트 표시) 충족. `npm test`(87개) 전부 통과.

### Phase 5 — Gemini 연동 (2026-08-15)

등록 시 이미지 1장으로 스타일 태깅(`lib/ai/tagger.ts`), 편차 리포트+태그를 Gemini에 보내 매칭 심각도·피드백 문장을 받는 것(`lib/ai/advisor.ts`)까지가 목표였다. **최종 판정은 항상 코드가 계산하고 Gemini는 서술만 한다**는 원칙(스펙 §9-10)을 그대로 지켰다.

**겪은 문제 — 스펙에 명시된 `gemini-2.5-flash`가 신규 발급 API 키에서 404**

**문제**: `GEMINI_API_KEY`를 새로 발급받아 태깅을 처음 실행하자 `ApiError: This model models/gemini-2.5-flash is no longer available to new users.`가 났다.

**원인**: 스펙 작성 시점(2026-08-13)에는 유효했던 모델이, 그 사이 Google이 신규 사용자 대상 접근을 막은 것으로 보인다. `client.models.list()`로 이 API 키가 실제로 쓸 수 있는 모델 목록을 직접 조회해보니 `gemini-2.5-flash`는 카탈로그에는 여전히 나열되지만(`generateContent` 호출 시점에만 별도로 막힘), `gemini-3.5-flash`·`gemini-3.7-flash`·`gemini-flash-latest` 등 더 최신 flash 계열은 정상 호출됐다.

**해결**: 사용자에게 상황을 알리고 대안(구체적 버전 고정 vs `gemini-flash-latest` 별칭)을 물어, `gemini-3.5-flash`로 교체하기로 했다. `CLAUDE.md`·설계 스펙(§4, §10)·계획 2 문서의 모델명을 전부 갱신해 실제 코드와 문서가 어긋나지 않게 했다. `/api/analyze`에서는 모델명을 문자열로 반복해서 하드코딩하지 않고 `GEMINI_MODEL` 상수를 참조하도록 해, 다음에 또 모델을 바꿔야 할 때 한 곳만 고치면 되게 했다.

**겪은 문제 2 — Gemini `responseSchema`는 표준 JSON Schema가 아니다**

**문제**: 구조화 출력 스키마의 `type` 필드에 JSON Schema 관례대로 `'object'`/`'string'`(소문자)을 썼는데, TypeScript는 통과했지만(`SchemaUnion = Schema | unknown`이라 타입 체크가 사실상 무력화됨) 실제로 스키마가 적용되는지 의심스러웠다.

**해결**: SDK 타입 정의(`node_modules/@google/genai/dist/genai.d.ts`)에서 `Type` enum을 확인했다 — Gemini의 스키마는 `Type.OBJECT`/`Type.STRING`처럼 대문자 상수를 쓴다. `lib/ai/tagger.ts`·`lib/ai/advisor.ts` 둘 다 `Type` enum으로 작성해 실제로 정상 동작하는 것까지 확인했다.

**겪은 문제 3 — Vitest mock에서 연속 실패(재시도) 테스트가 잡히는 예외를 unhandled rejection으로 오탐지**

**문제**: "한 번 실패하면 재시도하고, 재시도까지 실패하면 null" 테스트에서 `generateContentMock.mockRejectedValue(...)`(또는 `mockImplementation(() => Promise.reject(...))`)을 쓰면, 실제로는 `try/catch`로 잡히는 에러인데도 테스트가 `Error: timeout`으로 실패했다.

**원인**: 최소 재현으로 좁혀보니 `vi.mock()` 팩토리로 대체한 모듈을 통해 같은 mock 함수를 연속으로 두 번 호출·reject시키는 조합에서만 재현됐다 — `vi.fn()`을 직접 함수 인자로 넘기는 단순한 경우는 문제없었다. Vitest의 mock 계측(호출 결과 기록)과 이 조합이 상호작용해 실제로는 처리되는 rejection을 처리 안 된 것으로 오탐지하는 것으로 보인다.

**해결**: `mockRejectedValueOnce(...).mockRejectedValueOnce(...)`처럼 호출 횟수만큼 순서대로 등록하는 방식으로 바꾸니 정상 통과했다. `tests/ai/advisor.test.ts`와 계획 문서에 원인과 함께 남겨, 다음에 같은 패턴(vi.mock + 연속 reject)을 쓸 때 같은 삽질을 반복하지 않게 했다.

**검증**: 등록 시 실제 무신사 상품 이미지로 태깅을 실행해 `ai_tags`가 (카테고리·색상·톤·패턴·스타일 키워드·격식도·계절) 구조로 정확히 채워지는 것을 확인했다. `/api/analyze`에서는 정상 키로 호출해 4개 피드백 문장(요약·사이즈·매칭·가격)이 자연스러운 한국어로 나오고 `analyses.feedback`·`model`·`prompt_snapshot`이 채워지는 것을 확인한 뒤, `GEMINI_API_KEY`를 일부러 무효한 값으로 바꿔 같은 요청을 다시 보내 `feedback: null`·`fit_score`만으로 판정되는 폴백 경로(스펙 §12)까지 실제로 재현해 확인했다. 테스트로 생성한 옷·분석 결과는 검증 후 정리했다.

**결과**: 계획 2(선호도·핏 판단·Gemini·장바구니, 스펙 Phase 3~5) 완료. `npm test`(93개) 전부 통과, `npm run build` 타입 오류 없이 성공.

### Phase 6~8 — 공유 옷장 · 룩 · 마감 (2026-08-16)

옷장 공유 토글, 공개 열람(`/u/[share_slug]`), 로그인한 방문자의 추천 아이템 등록·룩 만들기, `/looks` 목록, 공유 링크 OG 이미지, 로딩 상태까지가 목표였다. 이걸로 스펙 §14의 Phase 0~8 전체가 끝난다.

**이번 Phase가 유난히 순조로웠던 이유**: 계획 1(Phase 1) 시점에 이미 `garments_insert` RLS 정책을 "본인 옷장에는 자유롭게, 남의 공개 옷장에는 `status='considering'`으로만" 넣도록 짜두었고, `garments` 테이블에도 `recommended_by`·`note` 컬럼을 처음부터 만들어뒀다. 그 덕분에 이번 계획에서 "친구 추천" 기능은 새 권한 로직을 거의 안 짜고, `lib/garments/register.ts`(계획 2)에 필드 두 개를 얹고 얇은 API 하나(`/api/recommend`)를 추가하는 것으로 끝났다 — 미리 해둔 설계 투자가 두 계획 뒤에 실제로 보상받은 사례였다.

**겪은 문제 — Satori(`next/og`)가 `{표현식}텍스트` 혼합 자식을 별도 노드로 갈라 500 에러를 냄**

**문제**: 공유 링크 OG 이미지(`app/u/[share_slug]/opengraph-image.tsx`)를 브라우저에서 직접 열어보니 500 에러였다. dev 서버 로그에는 `Expected <div> to have explicit "display: flex" ... if it has more than one child node`라는 원인이 찍혔는데, 정작 문제의 `<div>`에는 이미 `display: flex`를 명시해둔 상태였다.

**원인**: 에러가 가리킨 건 바깥쪽 컨테이너가 아니라 `<div>{nickname}님의 옷장</div>`처럼 JSX 표현식과 리터럴 텍스트가 섞인 안쪽 `<div>`였다. Satori(`next/og`의 렌더링 엔진)는 이 경우 `{nickname}`과 `"님의 옷장"`을 서로 다른 자식 노드 두 개로 쪼개서 받아들여, "자식이 2개 이상이면 명시적 `display`가 필요하다"는 검사에 걸린다 — 화면에는 이어 붙어 보이는 한 줄짜리 텍스트라도 Satori 입장에서는 복수 자식이었다.

**해결**: `{nickname}님의 옷장` → `{`${nickname}님의 옷장`}`처럼 템플릿 리터럴로 감싸 항상 텍스트 노드 하나만 전달되게 고쳤다. 표현식과 한글 텍스트를 같은 줄에 섞어 쓰는 흔한 패턴이 이 렌더링 엔진에서는 함정이라는 걸 실제로 500을 만나고서야 알았다.

**검증**: `outfits`/`outfit_items` RLS는 5개 시나리오(공개 옷장에 타인이 룩 생성, 그 옷장 소유가 아닌 옷은 룩에 못 넣음, 본인 옷장이어도 비공개면 룩을 못 만듦 — 스펙 §7 원문 그대로 "본인 예외 없음"까지 확인, 옷장 주인의 삭제 권한)를 실제 원격 DB에 두 사용자로 검증했다. Chrome 확장이 세션 중 반복적으로 끊겨서, UI로 못 본 구간(추천 등록·룩 생성)은 `registerGarment`/`/api/outfits`를 테스트 계정과 실제 로그인 세션으로 직접 호출해 DB 반영과 롤백(옷장 소유가 아닌 옷이 섞이면 방금 만든 `outfits` 행을 지우는 로직)까지 확인했다. 연결이 복구된 뒤에는 실제 브라우저로 공유 링크 공개/비공개 전환(200/404), 본인 옷장 방문 시 추천·룩 섹션이 정확히 숨겨지는 것, `/looks`의 제작자·썸네일 렌더링, OG 이미지·메타 태그까지 눈으로 확인했다. 테스트로 만든 룩·추천 아이템은 검증 후 모두 정리했다.

**결과**: 계획 3(공유 옷장·친구 추천·룩, 스펙 Phase 6~8) 완료. `npm test`(98개) 전부 통과, `npm run build` 타입 오류 없이 성공. 이걸로 `docs/superpowers/specs/2026-08-13-buy-or-not-design.md`에 정의된 전체 범위(Phase 0~8)가 구현됐다.

### 계획 4 — 네비게이션 & 디자인 시스템 (2026-08-17)

스펙 범위(Phase 0~8) 구현이 끝난 뒤 직접 써보니 화면 간 이동 수단이 전혀 없고(옷장 상세에서 뒤로 갈 방법도 없음), UI가 Tailwind 기본 회색·검정뿐이었다. `app/(app)/` 라우트 그룹으로 PC 헤더/모바일 하단 탭바를 공통 적용하고, `docs/superpowers/specs/2026-08-16-nav-and-design-system.md`에서 정한 웜톤 디자인 토큰(`--color-canvas` 등)과 공통 프리미티브(`Button`, `CARD_SURFACE`, `pillClass`)로 기존 화면 전체의 하드코딩 색을 걷어내는 게 목표였다.

**겪은 문제 — CSS 주석 안의 `*/`가 주석을 일찍 끊어버림**

**문제**: `app/globals.css`의 `@theme` 블록 위에 설명 주석을 달았는데, `npm run build`가 "Unexpected token Delim('*')"라는 CSS 파싱 경고를 냈다.

**원인**: 주석 문장 안에 "`--color-*` 를 `bg-*/text-*/border-*` 로"처럼 별표를 코드 표기용으로 섞어 썼는데, 그중 `*/` 시퀀스가 CSS 블록 주석의 닫는 토큰과 우연히 일치해 주석이 의도한 위치보다 훨씬 앞에서 끝나버렸다. 이후 실제 코드로 남아있던 주석 나머지 텍스트를 Lightning CSS가 CSS 문법으로 파싱하려다 걸린 경고였다.

**해결**: 주석 문장에서 `*/`가 만들어지는 조합(`-*` 뒤에 바로 `/`)을 피해 "`--color-x`를 `bg-x`, `text-x`, `border-x`로"처럼 풀어 썼다. CSS 파일에 주석을 달 때는 별표(`*`)와 슬래시(`/`)가 붙는 조합 자체를 조심해야 한다는 걸 실제 경고로 확인했다.

**겪은 문제 — Windows에서 `git mv`로 디렉터리 전체를 옮기지 못함**

**문제**: 인증 화면들을 `app/(app)/` 라우트 그룹으로 옮기려고 `git mv app/wardrobe "app/(app)/wardrobe"`(디렉터리 단위)를 실행했더니 "Permission denied"로 실패했다. `app/analyze`·`app/cart`·`app/looks`는 같은 명령으로 문제없이 옮겨졌는데 `app/wardrobe`만 실패했다.

**원인**: Windows에서는 파일시스템 핸들이 열려 있는 디렉터리(이 경우 `next dev`가 감시 중이던 디렉터리로 추정)를 통째로 rename하는 동작이 막힐 수 있다 — 유닉스 계열과 달리 디렉터리 이동이 원자적 rename 한 번으로 안전하게 처리되지 않는다.

**해결**: 디렉터리째 옮기는 대신 `git mv app/wardrobe/page.tsx "app/(app)/wardrobe/page.tsx"`처럼 파일 단위로 하나씩 옮기고, 빈 디렉터리만 `rmdir`로 정리했다. 파일 단위 이동은 Git 이력(rename으로) 그대로 보존됐다.

**겪은 문제 — 아이콘 라이브러리 export 이름을 Windows 파일시스템이 잘못 검증해줌**

**문제**: 계획 단계에서 `@phosphor-icons/react`가 실제로 `Coathanger`라는 이름을 export하는지 미리 확인했고(파일 존재 확인으로 "OK") 그 이름으로 코드를 작성했는데, 정작 `npm run build`에서 "Export Coathanger doesn't exist ... Did you mean to import CoatHanger?"로 실패했다.

**원인**: 실제 export 이름은 `CoatHanger`(H 대문자)였다. 사전 검증에 쓴 `ls node_modules/.../Coathanger.d.ts` 같은 파일 존재 확인이 Windows의 대소문자 구분 없는 파일시스템 때문에 실제로는 다른 대소문자의 `CoatHanger.d.ts`와 매치되어 "존재함"으로 잘못 통과됐다 — 파일 존재 여부만으로는 정확한 대소문자를 검증할 수 없었다.

**해결**: 빌드 에러가 정확한 이름을 알려줘서 `CoatHanger`로 고쳤다. 이후 나머지 아이콘 이름들은 `grep -x`(정확히 일치하는 줄만 매치)로 대소문자까지 정확히 비교해 재검증했다.

**검증**: 로그인한 브라우저로 PC 폭에서 헤더의 5개 메뉴(옷장·살까말까·장바구니·룩·마이페이지)를 모두 클릭해 실제 이동과 활성 탭 강조를 확인하고, 옷장 상세(`/wardrobe/[id]`)처럼 하위 경로에서도 "옷장" 탭이 계속 켜져 있는 것(Task 2에서 TDD로 만든 `isActiveNav`가 실제로 하는 일)을 확인했다. `/mypage`에서 구글 아바타·닉네임 표시, 공유 토글 이전, 로그아웃 후 `/`로 리다이렉트, 비로그인 상태로 `/wardrobe` 직접 접근 시 리다이렉트까지 실제 계정으로 로그아웃·재로그인해가며 확인했다. `/analyze`에서 실제 무신사 링크로 판정까지 끝까지 진행해 새 배지·편차 표 스타일을 확인한 뒤 테스트로 만든 분석 데이터는 정리했다. `/u/[share_slug]`는 로그인 상태(내 옷장으로 링크)와 `curl`로 뜬 비로그인 상태(로그인 링크) 양쪽을 확인했다. 모바일 하단 탭바는 이 세션의 브라우저 자동화 도구가 뷰포트 리사이즈를 반영하지 못해 픽셀 단위로는 확인하지 못했다 — 대신 `md:hidden`/`hidden md:block`이라는 표준 Tailwind 브레이크포인트 클래스가 정확히 적용됐음을 코드로 확인했다.

**결과**: 계획 4(네비게이션 & 디자인 시스템, `docs/superpowers/plans/2026-08-16-plan4-nav-and-design-system.md`) 1단계 완료. `npm test`(103개) 전부 통과, `npm run build` 타입 오류 없이 성공. 핏 판단 설정(2단계)은 마이페이지에 진입점만 만들어두고 별도 스펙·계획으로 이후 진행한다.

### 부록 — 카테고리 오분류 버그 수정 (2026-08-17)

계획 4를 쓰던 중 실제로 옷장을 써보다가 발견한 문제: 팔찌를 등록했더니 "상의"로 저장됐고, 신발·액세서리를 등록할 때도 총장·가슴단면 같은 상의용 실측 입력칸이 그대로 떴다.

**원인**: `lib/musinsa/parser.ts`의 `MUSINSA_CATEGORY_MAP`이 무신사 대분류명 중 일부(주얼리 등 액세서리 세부 분류로 추정)를 몰라 파싱이 실패했고, `components/GarmentForm.tsx`가 파싱 실패 시 기본값을 `'top'`으로 채워 넣어서 사용자가 놓치면 그대로 저장됐다. 실측 입력 폼도 `FIT_RULES`에 `shoes`/`acc`가 원래 없다는 걸 폼이 반영하지 않고 모든 카테고리에 무조건 그렸다.

**해결**: (1) 실측 "Field"를 `category in FIT_RULES`일 때만 렌더링해 신발·액세서리에는 아예 안 뜨게 했다. (2) 카테고리 파싱 실패 시 기본값을 `'top'`에서 `'acc'`로 바꿨다 — 잘못 등록돼도 핏 판단(FIT_RULES에 없는 카테고리는 채점을 건너뜀)을 오염시키지 않는 안전한 카테고리로 떨어지게 한 것이다. 팔찌의 실제 무신사 대분류명을 확인 못 해 `MUSINSA_CATEGORY_MAP` 자체는 넓히지 않았다 — 이 프로젝트가 Phase 0부터 지켜온 "실제 페이지로 검증 후 반영" 원칙상 근거 없이 카테고리명을 추측해 넣지 않았다.

**검증**: 브라우저에서 카테고리 드롭다운을 "액세서리"로 바꾸면 실측 섹션이 즉시 사라지고, "상의"로 되돌리면 즉시 다시 나타나는 것을 확인했다.

**결과**: `npm test`(103개) 전부 통과. `MUSINSA_CATEGORY_MAP` 확장은 실제 액세서리 상품 링크를 확보하면 이어서 진행하기로 계획 5 문서에 남겨뒀다.

### 계획 5 — 핏 판단 강도 설정 (2026-08-17)

`lib/fit/rules.ts`의 `FIT_RULES`는 카테고리별 14개 항목 × (허용편차·심각도·가중치) 총 45개 숫자로 이뤄져 있어 전부를 설정 화면에 노출하기엔 과했다. 그래서 허용편차에만 곱해지는 배율 하나(0.5배~2.0배 슬라이더)로 범위를 좁혔다. `docs/superpowers/specs/2026-08-17-fit-strictness-design.md`가 설계 근거다.

이번 계획은 겪은 문제보다 미리 잡아둔 설계 제약이 실제로 효과가 있었던 사례에 가깝다.

**설계 판단 1 — `buildPreferenceProfile`과 `scoreDeviation`에 반드시 같은 배율을 넘겨야 한다는 제약**

핏 판단은 "선호 실측값들을 허용편차 이내로 묶어 구간을 만드는 단계"(`buildPreferenceProfile`)와 "후보 옷이 그 구간 밖으로 얼마나 벗어났는지 재는 단계"(`scoreDeviation`)로 나뉘는데, 둘 다 같은 허용편차를 `[lo-t, hi+t]` 확장구간 계산에 쓴다. 배율을 두 곳에 각자 다르게 넘기면 이 전제가 깨진다는 걸 스펙 단계에서 미리 인지해, `/api/analyze`가 `profiles`에서 배율을 딱 한 번만 읽어 양쪽에 같은 값을 넘기도록 계획을 짰다. 실제 구현·종단 검증까지 이 부분에서 별도로 발견된 버그는 없었다 — 처음부터 제약으로 못박아 둔 게 그대로 맞아떨어진 경우다.

**설계 판단 2 — 슬라이더 저장을 `onChange`가 아니라 `onPointerUp`/`onKeyUp`에서 하기**

`<input type="range">`의 `onChange`는 드래그하는 동안 값이 바뀔 때마다 발생한다. 화면 표시는 `onChange`로 즉시 갱신하되 저장(`PATCH /api/profile`)은 손을 뗄 때만 하도록 나눴다. 브라우저 네트워크 탭으로 직접 확인한 결과, 드래그 한 번(수십 번의 값 변화)에 PATCH가 정확히 1번만 나갔다.

**검증**: 상의 3벌(총장 60·62·72.5cm, 별점 5점)을 등록해 선호 구간이 `[60,62]`와 `[72.5,72.5]` 두 클러스터로 나뉘게 만든 뒤, 총장 65cm인 후보 옷을 강도 0.5배와 2.0배로 각각 판단했다. 0.5배(허용편차 1.5cm)에서는 "1.5cm 초과"로 주의 판정이, 2.0배(허용편차 6.0cm)에서는 "적합"으로 살만함 판정이 나왔다 — 계획 문서에 미리 손으로 계산해 둔 값과 정확히 일치했다. 검증용으로 만든 옷·분석 데이터는 확인 후 정리했고, 슬라이더는 기본값(1.0배)으로 되돌려 뒀다.

**결과**: 계획 5(핏 판단 강도 설정, `docs/superpowers/plans/2026-08-17-plan5-fit-strictness.md`) 완료. `npm test`(107개) 전부 통과, `npm run build` 타입 오류 없이 성공.

### 계획 6 — 코드 구조 정리 (2026-08-17)

스펙 범위(Phase 0~8)와 계획 4·5까지 끝난 뒤, 이후 진행할 6개 하위 프로젝트(A~F) 중 첫 번째로 코드 구조를 정리했다. `LinkInputBar`·`AnalyzeLinkBar`·`RecommendLinkBar` 세 파일에 계획 3·4에서 두 번 "rule of three"로 기록만 해두고 미뤄뒀던 중복 약 90줄(무신사 링크 파싱 상태·API 호출·입력 UI)을 `useMusinsaParse` 훅과 `MusinsaLinkInput` 컴포넌트로 뽑아내고, `components/` 최상위 17개 파일을 도메인별 4개 폴더(`garment`/`analyze`/`share`/`account`)로 정리하는 순수 리팩터링이었다. 설계 근거는 `docs/superpowers/specs/2026-08-17-component-structure-design.md`.

**설계 판단 1 — 사용자가 요청한 `App.jsx`를 만들지 않은 이유**

사용자는 "React 컨벤션대로 폴더링하고 `App.jsx`는 컴포넌트를 조립하는 형태로 가볍게 해달라"고 요청했다. 하지만 이 프로젝트는 Create React App·Vite 같은 SPA가 아니라 Next.js App Router라서 `App.jsx`라는 파일 자체가 없고 만들 이유도 없다 — "화면은 조립만 하고 가볍게"라는 의도는 이미 `app/layout.tsx`와 각 `page.tsx`가 맡고 있다(예: `mypage/page.tsx`는 데이터를 가져와 `ShareToggle`·`FitStrictnessSlider`·`LogoutButton`을 배치하는 게 전부다). 요청을 문자 그대로 따르지 않고 왜 이 패턴이 안 맞는지 설명해 범위를 "중복 제거 + 폴더 정리"로 바로잡은 뒤 진행했다.

**설계 판단 2 — 스펙에 적어둔 것과 달리 `onStart` 콜백은 두 컴포넌트 모두에 필요했다**

스펙 초안에는 "새 링크를 파싱하기 시작할 때 이전 결과를 지우는" `onStart` 콜백이 `RecommendLinkBar`(추천 완료 메시지)에만 필요하다고 적었다. 그런데 계획을 쓰며 `AnalyzeLinkBar`의 실제 코드를 다시 읽어보니 그쪽 `handleSubmit`도 시작할 때 `setResult(null)`을 하고 있어 판단 결과 역시 지워야 했다 — 즉 세 파일 중 둘 다 필요했고 `LinkInputBar`만 필요 없었다. 문서를 먼저 쓰고 코드를 나중에 보면 이런 게 새는구나 싶어, 스펙을 고쳐 별도 커밋(`8c088c5`)으로 남겼다.

**설계 판단 3 — `MusinsaLinkInput`을 `div`가 아니라 프래그먼트로 감싼 이유**

부모 컴포넌트의 `space-y-4`는 `<form>`·에러 문구·`<GarmentForm>`을 각각 직접 자식으로 보고 사이 간격을 준다. 새로 뽑아낸 `MusinsaLinkInput`이 `<form>`과 에러 문구를 `<div>`로 한 번 더 감싸면 셋이 두 덩어리로 묶여 지금과 화면 간격이 달라진다. "기능 변화는 0"이 이 계획의 유일한 성공 기준이었던 만큼, 겉으로 티가 잘 안 나는 이런 차이가 오히려 놓치기 쉬웠다.

**설계 판단 4 — 로직 변경(Task 1)과 경로 변경(Task 2)을 서로 다른 커밋으로 나눈 이유**

Task 2는 24개 파일 48줄의 순수 기계적 import 경로 치환이다. 여기에 Task 1의 로직 diff가 섞이면 "이 줄이 실제로 동작을 바꾼 건지, 그냥 경로만 바뀐 건지"를 리뷰어가 한눈에 구분할 수 없다. `refactor: extract shared musinsa link parsing into hook and input`(`12f21f6`)과 `refactor: group components into domain folders`(`7d13fa5`)로 나눠 커밋했다.

**검증**: 컴포넌트·훅을 렌더링해 테스트할 jsdom·React Testing Library가 이 프로젝트에 없어(Vitest가 `environment: 'node'`), 이 계획엔 새 단위 테스트가 없다. 대신 `npm run build`(순수 구조 변경에서는 import 경로가 하나라도 틀리면 반드시 걸리는, 오히려 제일 강력한 검증 수단)와 기존 107개 회귀 테스트, 그리고 브라우저 수동 검증을 조합했다. 옷장 등록(`/wardrobe`)·구매 판단(`/analyze`)은 실제 무신사 링크로 파싱→폼→저장까지 끝까지 진행했고, `onStart`가 이전 결과(판단 결과·추천 완료 메시지)를 새 링크 파싱 시작과 동시에 지우는 것도 확인했다. 친구 추천(`RecommendLinkBar`)은 로그인 계정과 옷장 주인이 같으면 컴포넌트 자체가 렌더링되지 않는 조건(`user.id !== profile.id`)이 있어, 두 번째 계정으로 직접 전환해 사용자가 같은 방식으로 확인해 줬다. 폴더 이동(Task 2) 이후에는 `/wardrobe`·`/analyze`·`/cart`·`/looks`·`/mypage`·`/u/[share_slug]`를 열어 콘솔 에러 없이 렌더링되는 것을 확인했고, 옛 경로(`@/components/대문자로시작`)를 가리키는 import가 하나도 남지 않았음을 `grep`으로 재확인했다. 검증 중 만든 옷장·추천 테스트 데이터는 정리했다.

**결과**: 계획 6(코드 구조 정리, `docs/superpowers/plans/2026-08-17-plan6-component-structure.md`) 완료. `npm test`(107개) 전부 통과, `npm run build` 타입 오류 없이 성공. 합의한 A~F 중 A만 끝났고, 나머지(B 등록·삭제 UX 개선, C 수동 등록, D 추천→룩 흐름, E 핏 판단 정밀화, F 가격 인하 표시)는 각자 별도 스펙·계획 사이클로 이어서 진행한다.

### 계획 7 — 등록·삭제 UX 개선 (2026-08-17)

합의한 A~F 중 B. 링크 입력 폼을 닫는 기능, 실측표 사이즈 라벨 매칭 유연화(`"2 (L)"` ↔ `"L"`), 삭제 버튼 아이콘화, 장바구니 선택·전체 삭제 네 가지를 한 계획으로 묶어 진행했다. 설계 근거는 `docs/superpowers/specs/2026-08-17-registration-deletion-ux-design.md`.

**설계 판단 1 — "폼 접기"에 새 메서드가 필요 없어진 과정**

처음엔 "URL은 남기고 폼만 닫는" `collapse()`를 계획 6의 `useMusinsaParse`에 추가하려 했다. 그런데 사용자에게 확인해보니 "URL도 같이 지워달라"는 답이 와서, 그 동작이 계획 6에서 이미 만들어 둔 `reset()`(URL·파싱 결과를 모두 비움)과 정확히 같아졌다. 새 코드 없이 `GarmentForm`에 `onCancel` prop 하나만 추가하고 세 화면이 `parse.reset`을 그대로 넘기는 것으로 끝났다 — 짧은 설계 대화 한 번이 API 하나를 통째로 없앤 사례다.

**겪은 문제 — `<form>` 안의 `<button>`은 type을 안 주면 제출 버튼이 된다**

접기 버튼을 처음 `<button onClick={onCancel}>`로만 짰다면 클릭 시 폼이 그대로 제출됐을 것이다. HTML 스펙상 `<form>` 내부 `<button>`의 기본 `type`은 `submit`이기 때문이다. 계획을 쓰는 단계에서 이 점을 미리 확인해 `type="button"`을 명시했고, 실제 브라우저 검증에서도 접기를 눌렀을 때 옷이 등록되지 않는 것을 확인했다.

**겪은 문제 — `Button`에 넘긴 `className`이 내부 패딩을 못 이긴다**

아이콘 삭제 버튼을 정사각형으로 만들려고 `<Button className="p-2">`를 계획하다가, 이 프로젝트에 `tailwind-merge`가 없다는 걸 발견했다. `Button`은 `` `${BASE} ${className}` ``처럼 클래스 문자열을 이어붙이기만 하는데, 브라우저는 CSS를 소스 코드의 문자열 순서가 아니라 스타일시트에 실제로 쓰인 순서로 적용한다. Tailwind가 만드는 정적 CSS에서 `BASE`의 `px-4 py-2`가 나중에 정의돼 있으면, `className`으로 넘긴 `p-2`가 나중에 와도 이긴다는 보장이 없다 — 실제로 확인해 보니 기존 `CartItemCard`의 `px-3`도 이미 조용히 무시되고 있었다. 이번엔 아이콘 버튼에 패딩을 아예 안 넘기는 방식으로 피해 갔다. `Button`의 크기를 정말 바꿔야 할 일이 생기면 `className` 대신 `size` variant를 추가하는 게 맞다.

**설계 판단 2 — 사이즈 매칭에 substring이 아니라 토큰 교집합을 쓴 이유**

`"2 (L)".includes("L")`은 참이지만 `"XL".includes("L")`도 참이라, 문자열 포함 검사로는 서로 다른 사이즈("L"과 "XL")가 매칭돼 버린다. `extractSizeTokens`가 영문을 한 글자씩이 아니라 덩어리(`/[A-Z]+|\d+/g`) 단위로 잘라내, `"XL"`과 `"L"`이 애초에 다른 토큰이 되게 했다. 정규식 하나의 그룹 경계 선택이 오매칭 여부를 갈랐다.

**설계 판단 3 — 스펙엔 없던 `findMatchingSize`를 추가로 만든 이유**

스펙은 `sizesMatch` 하나만 정의하고 컴포넌트가 직접 `.find()`를 돌리게 했지만, 계획에서는 "정확히 같은 라벨을 먼저 찾고, 없을 때만 토큰 일치로 넓힌다"는 규칙까지 `findMatchingSize` 안에 넣었다. 이렇게 하면 컴포넌트가 실제로 내리는 결정 전체가 순수 함수 하나로 떨어져 단위 테스트(16개)로 다 검증할 수 있고, 후보가 여럿일 때도 정확한 라벨을 놓치지 않는다.

**설계 판단 4 — 장바구니 벌크 삭제에 새 API를 안 만든 이유**

`DELETE /api/garments/:id`가 이미 RLS로 소유자를 검증하며 계획 1부터 있었다. "선택 삭제"·"전체 삭제"는 이 엔드포인트를 `Promise.all`로 병렬 호출하는 것으로 끝냈다 — 개인 옷장 규모(많아야 수십 개)에서 요청 수는 문제가 안 되고, 서버 코드를 한 줄도 안 늘렸다.

**설계 판단 5 — 선택 상태를 `CartItemCard`가 아니라 `CartList`가 드는 이유**

"어느 카드가 선택됐는지"는 카드 한 장이 알 수 있는 정보가 아니라 여러 카드에 걸친 정보다. `/cart` 페이지 자체는 서버 컴포넌트라 상태를 못 들기 때문에, 상태가 필요한 만큼만 `CartList`라는 클라이언트 컴포넌트로 잘라냈다. `CartItemCard`는 `checked`/`onToggle`을 prop으로 받기만 하고 여전히 자기 데이터(이름·브랜드·판정)만 그린다.

**검증**: `sizeMatch.ts`는 TDD로 진행해 실패하는 테스트(모듈 없음) → 최소 구현 → 통과를 확인했다(16개). 나머지는 계획 6과 같은 이유로 `npm run build`(123개 타입 검사) + `npm test`(123개 회귀) + 브라우저 수동 검증을 조합했다. 폼 접기는 `/wardrobe`·`/analyze`에서 링크를 불러온 뒤 접었을 때 URL과 폼이 함께 사라지고 옷이 등록되지 않는 것을 확인했다. 사이즈 매칭은 실제 브라우저 클립보드 붙여넣기를 자동화로 재현하기 어려워, `DataTransfer`로 합성 클립보드(`<li>L</li>` + 실측 `<table>`)를 만들어 `paste` 이벤트를 실제 `PasteSizeTableField`에 디스패치하는 방식으로 검증했다 — 사이즈칸에 `"2 (L)"`을 넣고 라벨이 `"L"`뿐인 표를 붙였을 때 "2 (L) 사이즈 값이 자동으로 채워졌습니다"가 뜨는 것을 확인했다. 삭제 아이콘 버튼은 검증용 옷을 새로 등록해 2단계 확인 흐름과 실제 삭제까지 거쳐 정리했다. 장바구니는 세 아이템을 담아 체크박스 상시 노출, "N개 선택됨" 표시 전환, 선택 삭제(1개만 지워지고 나머지는 남는지), 전체 삭제(장바구니가 완전히 비는지)까지 전부 실제로 눌러 확인했다.

**결과**: 계획 7(등록·삭제 UX 개선, `docs/superpowers/plans/2026-08-17-plan7-registration-deletion-ux.md`) 완료. `npm test`(123개) 전부 통과, `npm run build` 타입 오류 없이 성공. 이걸로 합의한 A~F 중 B까지 끝났다. 다음은 C(수동 등록)다.

### 계획 8 — 수동 등록 (2026-08-17)

합의한 A~F 중 C. 무신사 링크가 없는 보세 옷도 옷장(`/wardrobe`)·구매 판단(`/analyze`)에 등록할 수 있게 했다. 설계 근거는 `docs/superpowers/specs/2026-08-17-manual-registration-design.md`.

**설계 판단 1 — 새 폼을 만들지 않고 기존 `GarmentForm`을 재사용한 것**

처음엔 탭 UI와 별도 컴포넌트로 설계했지만, 사용자가 "무신사 상품 등록 폼 그대로 쓰고 사진만 넣을 수 있게 하면 될 것 같다"고 정정해줘서 훨씬 단순해졌다. `GarmentForm`은 파싱이 전부 실패하면 이미 완전 수동 입력 폼이 되도록 짜여 있었다(계획 1) — "링크 없이도" 그 상태를 바로 트리거하는 합성 `ParseResult`(`createManualParseResult`, `goodsNo`는 `crypto.randomUUID()`) 하나만 추가하면 충분했다.

**설계 판단 2 — `goodsNo`에 실제 상품번호 대신 UUID를 쓴 이유**

DB 컬럼(`garments.goods_no`)은 nullable이지만, `registerGarment`의 이미지 저장 경로·중복 검사 로직은 값이 있다는 전제로 짜여 있다. null로 두고 그 로직들을 전부 예외 처리하는 대신, 실제로 상품번호인지 검증하는 곳이 코드베이스 어디에도 없다는 걸 계획 단계에서 확인하고 합성 UUID를 그대로 태웠다. 덕분에 `computeParseMode`도 수정 없이 정확히 `'manual'`을 계산했다.

**겪은 문제 — 사진 업로드 API를 따로 뒀더니 `registerGarment`가 같은 사진을 두 번 저장했다**

`/api/garments/upload-image`로 미리 올린 사진 URL을 `GarmentForm`이 넘기면, `registerGarment`는 그 URL이 어디서 왔는지 모르고 무조건 `copyImageToStorage`(URL을 내려받아 다시 업로드)를 불렀다. 실제로 첫 등록을 검증해보니 최종 `image_url`이 업로드 API가 만든 `manual/{uid}/...` 경로가 아니라 `copyImageToStorage`가 새로 만든 `{goodsNo}/{colorHash}.png` 경로였다 — 사진이 버킷에 두 벌 저장되고 원본은 아무 데서도 참조되지 않는 채로 남아 있었다. `copyImageToStorage` 맨 앞에 "이미 우리 버킷 공개 URL이면 그대로 쓴다"는 조건 하나를 추가해 고쳤고, 고친 뒤 다시 등록해 `image_url`이 `manual/...` 그대로 유지되는 것을 DB 쿼리로 확인했다.

**설계 판단 3 — URL을 문자열로 직접 조립하지 않고 SDK로 얻은 이유**

이중 업로드 방지 조건(`imageUrl.startsWith(...)`)에 쓸 접두어를 `${NEXT_PUBLIC_SUPABASE_URL}/storage/v1/object/public/garments/`처럼 손으로 이어붙이려다, 환경변수에 트레일링 슬래시가 있는지 없는지 같은 사소한 차이로 어긋날 수 있다는 걸 깨닫고 `supabaseAdmin.storage.from(BUCKET).getPublicUrl('').data.publicUrl`로 SDK가 실제로 만드는 값을 그대로 가져다 썼다.

**겪은 문제 — 브라우저 자동화로 업로드 검증용 이미지 파일을 만들어야 했다**

로컬에 마침 맞는 크기의 테스트 이미지가 없어서, 1×1 픽셀 PNG(정상 케이스)·5MB 더미 PNG(용량 초과 케이스)·가짜 GIF(허용 안 되는 타입 케이스) 세 개를 Node 스크립트로 즉석에서 만들어 각각 업로드해봤다. 세 경우 모두 정확한 에러 문구("4MB 이하 파일만 올릴 수 있습니다.", "JPG·PNG·WEBP 파일만 올릴 수 있습니다.")가 뜨고, 정상 파일만 실제로 등록까지 이어지는 것을 확인했다.

**검증**: `manualParseResult.ts`는 TDD로 진행해 실패하는 테스트(모듈 없음) → 최소 구현 → 통과를 확인했다(3개). 나머지는 앞선 계획들과 같은 이유로 `npm run build` + `npm test`(126개) + 브라우저 수동 검증을 조합했다. "직접 등록하기"를 눌러 링크 입력 없이 바로 빈 폼이 뜨는 것, "접기"로 원래 화면에 돌아가는 것, 카테고리를 "상의"로 바꾸면 실측 섹션이 즉시 나타나는 것(계획 6에서 만든 `hasMeasurableFit` 로직이 합성 `ParseResult`에서도 그대로 동작함)을 확인했다. 사진 업로드는 위 세 가지 케이스(정상·용량 초과·타입 불허)를 전부 실제로 눌러봤고, DB·Storage 쿼리로 최종 `image_url`과 저장된 오브젝트 경로까지 확인했다. 검증용으로 만든 옷 2벌은 삭제 버튼으로 정리했다.

**결과**: 계획 8(수동 등록, `docs/superpowers/plans/2026-08-17-plan8-manual-registration.md`) 완료. `npm test`(126개) 전부 통과, `npm run build` 타입 오류 없이 성공. 이걸로 합의한 A~F 중 A·B·C가 끝났다. 다음은 D(추천 → 룩 흐름)다.

### 계획 9 — 추천 → 룩 흐름 (2026-08-17)

합의한 A~F 중 D. 친구가 옷장 주인에게 아이템을 추천하면, 그 자리에서 방금 추천한 아이템을 재료로 바로 룩을 짤 수 있게 했다. 설계 근거는 `docs/superpowers/specs/2026-08-17-recommend-to-outfit-design.md`.

**설계 판단 1 — DB·RLS를 하나도 안 건드린 이유**

`outfit_items_insert` RLS 정책(`supabase/migrations/0005_outfits.sql`)을 직접 읽어보니 `g.owner_id = o.wardrobe_owner_id`만 검사하고 `status`는 안 봤다. "룩에 넣을 수 있는 옷"과 "옷장에 실제로 소유한 옷(`status='owned'`)"이 이 프로젝트 설계상 처음부터 다른 개념이었다는 뜻이다 — 방금 추천으로 들어간 `status='considering'` 아이템도 이미 룩에 넣을 권한이 있었다. 덕분에 계획 전체가 마이그레이션 없이 프론트엔드 작업만으로 끝났다.

**설계 판단 2 — 상태 공유에 `sessionStorage`를 고른 이유**

"새로고침해도 남고, 이 페이지를 벗어나거나 룩을 제출하면 사라져야 한다"는 요구를, 서버 쿼리를 넓히지 않고 그대로 만족시킨다. 서버로 넓히지 않았기 때문에 "다른 방문자의 추천은 안 보여야 한다"는 조건도 따로 신경 쓸 필요가 없었다 — 애초에 서버 어디에도 남지 않고 이 브라우저 탭 안에만 있다. 컴포넌트 언마운트(페이지 이탈) 시 `useEffect` 클린업에서 `sessionStorage.removeItem`을 부르는 것으로 "이탈 시 정리"를, 룩 제출 성공 콜백에서 같은 걸 부르는 것으로 "제출 시 정리"를 처리했다.

**설계 판단 3 — `registerGarment`가 이미 계산해 둔 값을 반환하기만 한 것**

`finalImageUrl`(Storage 복사까지 끝난 최종 이미지 URL)은 계획 1부터 계산되고 있었는데 아무도 반환하지 않고 있었다. 새 쿼리나 계산 없이 `RegisterGarmentResult`에 `name`·`imageUrl` 필드 두 개를 얹어 돌려주는 것으로, 추천 직후 프론트가 룩 재료 목록에 바로 쓸 데이터를 얻었다.

**설계 판단 4 — `RecommendLinkBar`·`OutfitBuilder`의 새 prop을 전부 선택(optional)으로 둔 이유**

두 컴포넌트를 `RecommendAndBuild`라는 새 클라이언트 래퍼로 묶었지만, 계획 서두에 "`RecommendAndBuild` 없이 단독으로 쓰던 기존 방식이 그대로 동작해야 한다"는 제약을 걸어뒀다. 계획을 쓰며 `grep`으로 두 컴포넌트가 실제로 `page.tsx` 한 곳에서만 쓰인다는 걸 확인했지만(주석 속 언급 하나는 걸러냈다), 그래도 새 prop을 필수로 만들 이유가 없어 전부 선택으로 뒀다.

**검증**: 새 로직(타입 확장, `sessionStorage` 읽고 쓰기)이 전부 UI에 바로 연결되는 얇은 코드라 순수 함수 단위 테스트로 뽑을 게 마땅치 않았다. `npm run build` + `npm test`(126개, 변화 없음 — 이 계획은 새 테스트를 추가하지 않았다)에 더해, 두 계정(옷장 주인 계정과 추천하는 친구 계정)으로 실제 흐름을 전부 눌러 확인했다: 무신사 링크로 추천 → "룩 만들기" 목록에 "추천함" 배지와 함께 자동 체크된 채로 나타나는지, 기존 옷을 더 골라 룩을 만들면 추천 목록이 비워지는지, 제출 전 새로고침에도 추천 목록이 남아있는지, 다른 페이지로 갔다가 돌아오면 비어 있는지. 사용자가 직접 두 계정을 오가며 검증해 "확인됐어 문제없어"로 통과를 알려줬다.

**결과**: 계획 9(추천 → 룩 흐름, `docs/superpowers/plans/2026-08-17-plan9-recommend-to-outfit.md`) 완료. `npm test`(126개) 전부 통과, `npm run build` 타입 오류 없이 성공. 이걸로 합의한 A~F 중 A·B·C·D가 끝났다. 다음은 E(핏 판단 정밀화)다.

### 계획 10 — 항목별 허용오차 직접 입력 (2026-08-17)

합의한 A~F 중 E. 계획 5가 명시적으로 범위 밖에 남겨둔 세 가지(항목별 허용오차, 심각도·가중치, 판정 경계값) 중 항목별 허용오차만 다뤘다(사용자 확인 — 나머지 둘은 앱의 핵심 판정 신뢰도를 좌우해 개발자 제어로 남겼다). 설계 근거는 `docs/superpowers/specs/2026-08-17-fit-field-overrides-design.md`.

**겪은 문제 — `npx supabase db push`가 마이그레이션 이력 불일치로 막혔다**

**문제**: 이번 계획의 새 마이그레이션(`0007_fit_field_overrides.sql`)을 `npx supabase db push`로 적용하려 하자 `LegacyDbPushMissingLocalError`(원격에 있는 마이그레이션 버전이 로컬 디렉터리에 없다)로 실패했다.

**원인**: `supabase migration list`로 대조해보니, 계획 5의 마이그레이션(로컬 파일명 `0006_fit_strictness.sql`)이 실제로는 CLI의 `db push`가 아니라 이전 세션에서 Supabase MCP의 `apply_migration` 도구로 적용돼 있었다. 그 도구는 파일명이 아니라 타임스탬프(`20260817040253`)로 원격 이력에 기록한다 — 스키마 변경 자체(`fit_strictness` 컬럼)는 정상 반영됐지만, CLI가 기대하는 "로컬 0006 = 원격 0006" 버전 대응이 어긋나 있었다.

**해결**: `supabase migration repair`처럼 이력 자체를 고치는 명령은 쓰지 않았다 — 라이브 사용자 데이터가 있는 프로젝트의 마이그레이션 이력 테이블을 직접 손대는 건 계획에 없던 위험한 조작이다. 대신 0006이 실제로 적용됐던 것과 같은 경로(Supabase MCP `apply_migration`)로 0007을 적용했다. 로컬 `0007_fit_field_overrides.sql` 파일의 SQL을 그대로 실행했으므로 계획에 커밋된 내용과 실제 적용된 스키마가 정확히 일치한다.

**검증**: `information_schema.tables`로 테이블이 실제로 생겼는지 확인한 뒤 진행했다. 이 CLI/MCP 이력 불일치는 앞으로도 `db push`를 쓸 때마다 다시 나타날 수 있어(0006이 여전히 그 버전으로 남아있으므로), 다음에 CLI로 마이그레이션을 다뤄야 할 일이 생기면 `migration repair`가 필요하다는 걸 사용자에게 알렸다.

**설계 판단 — 계획 5의 `toleranceMultiplier` 파라미터 패턴을 그대로 확장한 것**

새 추상화(리졸버 함수, 별도 모듈 등)를 만들지 않고 `buildPreferenceProfile`·`scoreDeviation`에 넷째 파라미터 `fieldOverrides`를 추가하는 것으로 끝냈다. 기본값이 `{}`라 기존 호출부·테스트가 그대로 통과했다 — 계획 5가 이미 검증한 "기본값으로 하위 호환을 지키는" 확장 방식을 그대로 재사용한 것이다.

**검증**: 테스트 예시 수치를 코드로 옮기기 전에 손으로 먼저 계산했다 — "전체 배율 2배(허용구간 [54,68])면 통과할 65가, 항목별 override 1.5(허용구간 [58.5,63.5])로 고정하면 위반(1.5cm 초과, 2점)"이 되는 걸 계산해서 확인한 뒤 테스트로 옮겼다(TDD로 실패 → 구현 → 통과 확인, 3개). 새 테이블·새 RLS 정책이라 `tests/rls.test.ts`에 alice·bob 두 계정으로 실제 Postgres에 붙는 테스트도 추가했다(5개, 계획 3의 outfits 패턴을 그대로 따름). UI는 `/mypage`에서 카테고리 탭 전환, 항목별 입력 후 다른 칸으로 포커스를 옮겼을 때 저장되는지, 새로고침해도 값이 남아있는지, "기본값으로"를 누르면 실제로 지워지는지를 전부 눌러보고 DB 쿼리로 저장·삭제를 직접 대조했다.

**결과**: 계획 10(항목별 허용오차 직접 입력, `docs/superpowers/plans/2026-08-17-plan10-fit-field-overrides.md`) 완료. `npm test`(134개) 전부 통과, `npm run build` 타입 오류 없이 성공. 이걸로 합의한 A~F 중 A·B·C·D·E가 끝났다. 다음은 F(가격 인하 표시)다.

### 계획 11 — 장바구니 가격 인하 표시 (2026-08-17)

합의한 A~F 중 F, 마지막 사업. 장바구니(`status='considering'`)에 무신사 링크로 담아둔 옷의 가격이 떨어지면 "원래 얼마 → 지금 얼마"를 보여준다. 설계 근거는 `docs/superpowers/specs/2026-08-17-cart-price-drop-design.md`.

이번 계획을 실행하며 새로 겪은 문제는 없었다 — 마이그레이션 적용은 계획 10에서 이미 확인된 `npx supabase db push` 이력 불일치 문제라 같은 우회(Supabase MCP `apply_migration`으로 직접 적용)를 그대로 재사용했고, 코드는 `npm run build`·`npm test`가 매 태스크마다 첫 시도에 통과했다. 대신 계획 단계에서 이미 확정하고 실행하며 재확인한 설계 판단 네 가지를 남긴다.

**설계 판단 1 — Vercel Cron이 아니라 방문 시 지연 확인을 고른 이유**

이 프로젝트는 아직 실제 배포된 적이 없어, Cron 기반 설계는 지금 당장 검증할 방법이 없다(스펙 §2). `/cart` 방문 시 트리거하는 방식은 배포 여부와 무관하게 바로 만들고 실제로 눌러 테스트할 수 있다. 나중에 실제로 배포한 뒤 Cron이 필요해지면, 새로 만든 API(`POST /api/cart/refresh-prices`)를 그대로 Cron 핸들러에서 호출하면 되도록 로직을 라우트 하나에 다 몰아뒀다.

**설계 판단 2 — 가격 이력 테이블 대신 `garments`에 컬럼 두 개만 더한 이유**

사용자가 필요로 한 건 "원래→지금" 한 쌍이지 가격 변동 그래프가 아니었다. `garment_measurements`처럼 여러 값을 누적해야 하는 경우와, 계획 10의 `fit_field_overrides`처럼 "지금 값 하나"만 있으면 되는 경우를 이 프로젝트에서 계속 구분해왔는데(CLAUDE.md의 "정규화 테이블 vs JSONB/컬럼" 판단 기준과 같은 맥락), 이번에도 후자였다. `last_known_price`·`price_checked_at` 두 컬럼만 더했고, 기존 `garments_update` RLS 정책(`owner_id = auth.uid()`)이 그대로 커버해 새 정책도 필요 없었다.

**설계 판단 3 — 재확인이 실패해도 `price_checked_at`을 갱신하는 이유**

상품이 삭제됐거나 무신사 페이지 구조가 바뀌어 계속 파싱에 실패하는 링크가 있으면, 그 링크를 담은 사용자가 장바구니를 방문할 때마다 매번 헛수고로 무신사에 요청을 다시 보내게 된다. `refreshOne`이 파싱에 실패해도 `price_checked_at`만은 갱신하도록 짜서, 실패도 "확인은 했다"로 치고 다음 7일간은 같은 링크를 다시 건드리지 않는다.

**설계 판단 4 — 한 번의 방문에 최대 5개까지만 재확인하는 이유**

오랜만에 방문해 장바구니에 오래 안 본 상품이 여러 개 쌓여 있어도, 방문 한 번에 몰아서 다 긁지 않는다. `price_checked_at`이 오래된 순으로 최대 5개만 골라 처리하고, 나머지는 다음 방문에서 마저 처리된다 — 첫 로딩을 막지 않는 fire-and-forget 방식과 같은 이유로, 방문 한 번의 부담을 작게 유지하기 위한 판단이다.

**검증**: 새 파싱 로직은 만들지 않고 계획 1의 `fetchProductHtml`·`parseProductHtml`을 그대로 재사용했으므로 이번 계획에서 새 단위 테스트는 추가하지 않았다(`npm test` 134개, 계획 10과 동일). 대신 실제 무신사 링크(`musinsa.com/products/6593921`)로 장바구니 아이템을 등록해 전체 파이프라인을 눈으로 확인했다: `price_checked_at`을 8일 전으로 강제해 `/cart` 방문 시 `POST /api/cart/refresh-prices`가 실제로 나가는지(네트워크 탭), DB의 `last_known_price`·`price_checked_at`이 실제 무신사 현재가로 갱신되는지 SQL로 대조했다. 가격 인하 표시는 `last_known_price`를 `price`보다 4,000원 낮게 만들어 취소선 원가 → 강조된 인하가 + "인하" 배지가 뜨는 것을, 다시 `last_known_price = price`로 되돌려 인하가 없을 때는 가격 하나만 평범하게 뜨는 것을 화면으로 확인했다. 검증에 쓴 값은 확인 후 `null`로 정리했다.

**결과**: 계획 11(장바구니 가격 인하 표시, `docs/superpowers/plans/2026-08-17-plan11-cart-price-drop.md`) 완료. `npm test`(134개) 전부 통과, `npm run build` 타입 오류 없이 성공. 이걸로 합의한 A~F가 전부 끝났다. 남은 일은 이 문서들의 "남은 일" 절(액세서리 카테고리 확장, 휴지통, 수동 등록 확장, 실시간 알림, 심각도·가중치 조정, 실제 배포, Cron 자동화)에 정리돼 있으며, 새 사업이 필요해지면 그때 다시 브레인스토밍부터 시작한다.

### 계획 12 — 직접 등록 화면 안내문 제거 (2026-08-17)

A~F 완료 후 새로 제안된 6개 기능 중 첫 번째(진행 순서: 6→1→3→2→4→5로 합의). 설계 근거는 `docs/superpowers/specs/2026-08-17-manual-form-banner-fix-design.md`.

**문제**: "무신사 링크가 없나요? 직접 등록하기"로 들어간 완전 수동 입력 화면에도 "일부 정보를 자동으로 가져오지 못했습니다. 아래 표시된 칸만 채워주세요."라는 안내문이 떴다. 이 문구는 계획 1에서 무신사 링크를 파싱했는데 일부 필드만 실패했을 때를 위해 만든 것인데, "링크 없이 처음부터 전부 직접 입력하겠다"고 고른 사용자에게는 사실과 다른 안내였다.

**원인**: 계획 8이 도입한 `createManualParseResult()`(`lib/musinsa/manualParseResult.ts`)는 무신사 링크 없이도 기존 `GarmentForm`을 그대로 재사용하려고, 모든 필드를 `fail()` 처리한 합성 `ParseResult`를 만든다. 배너 조건(`manualFields.length > 0`)은 "필드 중 하나라도 실패"만 보고, "링크에서 왔는데 일부 실패"와 "링크 자체가 없어서 전부 실패"를 구분하지 못했다.

**해결**: `GarmentForm`이 이미 받고 있던 `sourceUrl: string | null` prop이 정확히 이 둘을 구분하는 신호였다(`LinkInputBar.tsx`에서 실제 링크 파싱이면 `parse.url`을, "직접 등록하기"면 `null`을 넘긴다). 새 prop이나 상태를 추가하지 않고 배너 조건에 `sourceUrl !== null`만 더했다.

**검증**: 조건문 한 줄 변경이라 별도 단위 테스트 대상은 없었고, `npm run build`(134개 타입 검사 그대로, 회귀 없음) + `npm test`(134개, 새 테스트 없음) + 브라우저 확인으로 검증했다. 실제 무신사 링크(`musinsa.com/products/6593921`, 사이즈 옵션 등 일부 필드가 파싱 실패하는 상품)로는 배너가 그대로 뜨는 것을, "직접 등록하기"로 연 완전 수동 폼에서는 배너가 뜨지 않는 것을 화면으로 확인했다.

**결과**: 계획 12(직접 등록 화면 안내문 제거, `docs/superpowers/plans/2026-08-17-plan12-manual-form-banner-fix.md`) 완료. `npm test`(134개) 전부 통과, `npm run build` 타입 오류 없이 성공. 다음은 1(옷장 선호도 미설정 표시)이다.

### 계획 13 — 옷장 카드 선호도 미설정 배지 (2026-08-17)

새로 제안된 6개 중 두 번째(진행 순서 6→1→3→2→4→5). 설계 근거는 `docs/superpowers/specs/2026-08-17-wardrobe-preference-badge-design.md`.

**설계 판단 1 — 별점만으로 "미설정"을 판단한 이유**

선호도는 별점·핏·착용빈도 세 항목이지만, 핏 판단 엔진(`lib/fit/profile.ts`)이 가장 핵심적으로 쓰는 신호는 별점이다(`rating>=4`/`<=2`가 성공/실패 집합을 가른다 — `PreferenceForm.tsx` 주석). 핏·착용빈도는 별점을 매기면 자연스럽게 따라오는 보조 정보로 보고, 세 항목을 전부 강제하지 않았다 — 안 그러면 등록 직후 사용자가 배지를 없애려고 불필요하게 여러 번 클릭해야 한다.

**설계 판단 2 — 카드 바깥 요소를 `Link`에서 `div`로 바꾼 이유**

배지도 상세 페이지의 특정 섹션(`#선호도`)으로 가는 별도 링크여야 하는데, 기존처럼 카드 전체가 `Link`였다면 그 안에 또 다른 `Link`를 넣는 게 되어 `<a>` 안에 `<a>`가 중첩되는 잘못된 HTML이 된다. 바깥을 `div`(카드 틀)로 바꾸고, 이미지+텍스트 블록은 그대로 `Link`(카드 클릭 = 상세 페이지 맨 위)로, 배지는 `rating == null`일 때만 뜨는 형제 `Link`(`#선호도` 앵커)로 나눠 이 문제를 피했다.

**설계 판단 3 — 공유 옷장에 영향이 없는 이유**

스펙을 쓰면서 실제로 `grep`해 확인했다 — `app/u/[share_slug]/page.tsx`(친구가 보는 공유 옷장)는 이 `GarmentCard`를 아예 쓰지 않고 자체 `PublicGarmentCard`를 따로 두고 있고, 쿼리에도 `rating`을 애초에 안 담아온다. 그래서 배지가 방문자에게 선호도 정보를 새로 노출할 위험이 없다는 걸 코드로 확인한 뒤 진행했다.

이번 계획을 실행하며 새로 겪은 문제는 없었다. 다만 브라우저 검증 중, 로그인돼 있던 브라우저 세션이 계획 9에서 두 계정 검증에 썼던 두 번째 테스트 계정(옷장이 비어 있던 계정)이었다는 걸 뒤늦게 알아차려, 그 계정에 "직접 등록하기"로 검증용 옷을 하나 만들어 배지 노출·앵커 이동·별점 매긴 뒤 배지 사라짐·일반 클릭 시 상세 페이지 맨 위 이동까지 전부 확인한 뒤 삭제로 정리했다.

**검증**: `GarmentCard`는 조건문 하나짜리 프레젠테이션 컴포넌트라 단위 테스트 대상이 없어(스펙에서 이미 명시), `npm run build`(134개 타입 검사 그대로) + `npm test`(134개, 새 테스트 없음, 회귀 없음) + 브라우저 확인으로 검증했다. 별점 없는 옷에 배지가 뜨는지, 배지를 누르면 `/wardrobe/{id}#선호도`로 이동해 선호도 섹션이 보이는지, 별점을 매기면 배지가 사라지는지, 카드의 나머지 영역을 누르면 여전히 상세 페이지 맨 위로 이동하는지 전부 실제로 눌러 확인했다.

**결과**: 계획 13(옷장 카드 선호도 미설정 배지, `docs/superpowers/plans/2026-08-17-plan13-wardrobe-preference-badge.md`) 완료. `npm test`(134개) 전부 통과, `npm run build` 타입 오류 없이 성공. 다음은 3(판단 리포트 재조회)이다.
