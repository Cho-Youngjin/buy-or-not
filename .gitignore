# Local / AI tooling — keep off GitHub for this portfolio repo
.agents/
.claude/

# Design reference kept only on the local machine
DESIGN-cursor.md

# Phase 0 Musinsa research scratch (HTML/JSON fixtures, not app code)
.scratch_phase0/

# dependencies
/node_modules

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — .env.local.example stays tracked as the template
.env
.env.local
.env*.local

# vercel
.vercel

# supabase CLI local link state (connection info, not source of truth)
supabase/.temp/
supabase/.branches/

# typescript
*.tsbuildinfo
next-env.d.ts

.superpowers/

# 앞으로 새로 만드는 파일부터만 적용 — 이미 커밋된 기존 docs/·tests/ 파일은 그대로 추적된다.
docs/
tests/
